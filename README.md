# Spatial Modelling for Australian Maximum Temperature


## Project Structure


### Notes

The deprecated EDA module contains code for the time series plot and the Hovmoller plots, which the EDA2 module does not contain. 

## Dependencies

### Packages for Data Processing

- `netcdf4`

- `sf`, `raster` and `terra`

- `ggplot2`

- `purrr`

- `dplyr` and `tidyr`

### Packages for Modelling

- `gstat` (required), `FRK`, and `GpGp`
